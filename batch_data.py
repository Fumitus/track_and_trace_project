def batch_data():
    """
    ENter batch data and store in variables"""
    name = input('Enter product name: ')
    batch = input('Enter product batch: ')
    expire = input('ENter product expire date: ')

    return name+batch+expire
    print('Product {} batch {} will expire in {}'.format(name,batch,expire))

# arba pabandyti taip KOLKAS NEVEIKIA

    class batch_data():
        def __init__(self, batch, expire, name):
            self.batch=input('Enter product batch number: ')
            if 7>len(batch)>7
            print('Please enter batch number of 8 numbers')
            self.expire=input('Enter product expire date: ')
            sefl.product_name=input('Enter product name: ')
        def __repr__():
            return '{}{}{}'.format(name, batch, expire)